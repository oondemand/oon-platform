apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${SERVICE_NAME}-cert
  namespace: ${NAMESPACE}
spec:
  secretName: ${SERVICE_NAME}-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${HOST}
